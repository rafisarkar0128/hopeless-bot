const { DiscordClient } = require("./DiscordClient");
const { LavalinkClient } = require("./LavalinkClient");
const { Logger } = require("./Logger");

module.exports = {
  DiscordClient,
  LavalinkClient,
  Logger,
};
